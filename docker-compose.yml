services:
  storeops:
    build: .
    image: storeops-harness:latest
    container_name: storeops-api
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      NODE_ENV: production
    restart: unless-stopped
